<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.theodoichitieu">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.report.CalculateIntertestActivity"></activity>
        <activity android:name=".view.report.MoneyExchangeActivity" />
        <activity android:name=".view.report.IncomePayReportActivity" />
        <activity android:name=".view.report.CatogoriesReportActivity" />
        <activity android:name=".view.report.IncomeReportActivity" />
        <activity android:name=".view.report.PayReportActivity" />
        <activity
            android:name=".view.AddAccountLineActivity"
            android:label="@string/title_activity_add_account_line"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".view.MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>